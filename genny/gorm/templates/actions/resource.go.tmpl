package actions

import (

  "github.com/pkg/errors"
  "github.com/gobuffalo/buffalo"
  "github.com/gobuffalo/pop"
  "{{.App.ModelsPkg}}"
)

// This file is generated by Buffalo. It offers a basic structure for
// adding, editing and deleting a page. If your model is more
// complex or you need more than the basic implementation you need to
// edit this file.

// Following naming logic is implemented in Buffalo:
// Model: Singular ({{.ModelName.Model}})
// DB Table: Plural ({{.ModelName.Table}})
// Resource: Plural ({{.Name.Resource}})
// Path: Plural (/{{.Name.URL}})
// View Template Folder: Plural (/templates/{{.FilesPath}}/)

// {{.Name.Resource}}Resource is the resource for the {{.ModelName.Model}} model
type {{.Name.Resource}}Resource struct{
  buffalo.Resource
}

// List gets all {{.ModelName.ModelPlural}}. This function is mapped to the path
// GET /{{.Name.URL}}
func (v {{.Name.Resource}}Resource) List(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  {{.ModelName.VarCasePlural}} := &models.{{.ModelName.ModelPlural}}{}

  // Paginate results. Params "page" and "per_page" control pagination.
  // Default values are "page=1" and "per_page=20".
  tN := tx.NewScope({{.ModelName.VarCasePlural}}).GetModelStruct().TableName(tx)
	var count int
	tx.Table(tN).Count(&count)

	p := paginate(c, count)

	q := tx.Limit(p.PerPage).Offset(p.Page - 1)

  // Retrieve all {{.ModelName.ModelPlural}} from the DB
  if err := q.Find(&{{.ModelName.VarCasePlural}}).Error; err != nil {
    return errors.WithStack(err)
  }

  // Add the paginator to the context so it can be used in the template.
  c.Set("pagination", p)

  return c.Render(200, r.Auto(c, {{.ModelName.VarCasePlural}}))
}

// Show gets the data for one {{.ModelName.Model}}. This function is mapped to
// the path GET /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Show(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.ModelName.Model}}
  {{.ModelName.VarCaseSingular}} := &models.{{.ModelName.Model}}{}

  // To find the {{.ModelName.Model}} the parameter {{.Name.ParamID}} is used.
  if err := tx.Where("id = ?", c.Param("{{.Name.ParamID}}")).First({{.ModelName.VarCaseSingular}}).Error; err != nil {
    return c.Error(404, err)
  }

  return c.Render(200, r.Auto(c, {{.ModelName.VarCaseSingular}}))
}

// New renders the form for creating a new {{.ModelName.Model}}.
// This function is mapped to the path GET /{{.Name.URL}}/new
func (v {{.Name.Resource}}Resource) New(c buffalo.Context) error {
  return c.Render(200, r.Auto(c, &models.{{.ModelName.Model}}{}))
}

// Create adds a {{.ModelName.Model}} to the DB. This function is mapped to the
// path POST /{{.Name.URL}}
func (v {{.Name.Resource}}Resource) Create(c buffalo.Context) error {
  // Allocate an empty {{.ModelName.Model}}
  {{.ModelName.VarCaseSingular}} := &models.{{.ModelName.Model}}{}

  // Bind {{.ModelName.VarCaseSingular}} to the html form elements
  if err := c.Bind({{.ModelName.VarCaseSingular}}); err != nil {
    return errors.WithStack(err)
  }

  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Validate the data from the html form
  verrs, err := {{.ModelName.VarCaseSingular}}.ValidateCreate(tx)
  if err != nil {
    return errors.WithStack(err)
  }

  if verrs.HasAny() {
    // Make the errors available inside the html template
    c.Set("errors", verrs)

    // Render again the new.html template that the user can
    // correct the input.
    return c.Render(422, r.Auto(c, {{.ModelName.VarCaseSingular}}))
  }

  // If there are no errors set a success message
  c.Flash().Add("success", "{{.ModelName.Model}} was created successfully")

  // and redirect to the {{.Name.URL}} index page
  return c.Render(201, r.Auto(c, {{.ModelName.VarCaseSingular}}))
}

// Edit renders a edit form for a {{.ModelName.Model}}. This function is
// mapped to the path GET /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}/edit
func (v {{.Name.Resource}}Resource) Edit(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.ModelName.Model}}
  {{.ModelName.VarCaseSingular}} := &models.{{.ModelName.Model}}{}

  if err := tx.Where("id = ?", c.Param("{{.Name.ParamID}}")).Find(&{{.ModelName.VarCaseSingular}}).Error; err != nil {
    return c.Error(404, err)
  }

  return c.Render(200, r.Auto(c, {{.ModelName.VarCaseSingular}}))
}

// Update changes a {{.ModelName.Model}} in the DB. This function is mapped to
// the path PUT /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Update(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.ModelName.Model}}
  {{.ModelName.VarCaseSingular}} := &models.{{.ModelName.Model}}{}

  if err := tx.Where("id = ?", c.Param("{{.Name.ParamID}}")).Find(&{{.ModelName.VarCaseSingular}}).Error; err != nil {
    return c.Error(404, err)
  }

  // Bind {{.ModelName.Model}} to the html form elements
  if err := c.Bind({{.ModelName.VarCaseSingular}}); err != nil {
    return errors.WithStack(err)
  }

  verrs, err := {{.ModelName.VarCaseSingular}}.ValidateUpdate(tx)
  if err != nil {
    return errors.WithStack(err)
  }

  if verrs.HasAny() {
    // Make the errors available inside the html template
    c.Set("errors", verrs)

    // Render again the edit.html template that the user can
    // correct the input.
    return c.Render(422, r.Auto(c, {{.ModelName.VarCaseSingular}}))
  }

  // If there are no errors set a success message
  c.Flash().Add("success", "{{.ModelName.Model}} was updated successfully")

  // and redirect to the {{.Name.URL}} index page
  return c.Render(200, r.Auto(c, {{.ModelName.VarCaseSingular}}))
}

// Destroy deletes a {{.ModelName.Model}} from the DB. This function is mapped
// to the path DELETE /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Destroy(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*gorm.DB)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.ModelName.Model}}
  {{.ModelName.VarCaseSingular}} := &models.{{.ModelName.Model}}{}

  // To find the {{.ModelName.Model}} the parameter {{.Name.ParamID}} is used.
  if err := tx.Where("id = ?", c.Param("{{.Name.ParamID}}")).Find(&{{.ModelName.VarCaseSingular}}).Error; err != nil {
    return c.Error(404, err)
  }

  if err := tx.Delete(&{{.ModelName.VarCaseSingular}}).Error; err != nil {
    return errors.WithStack(err)
  }

  // If there are no errors set a flash message
  c.Flash().Add("success", "{{.ModelName.Model}} was destroyed successfully")

  // Redirect to the {{.Name.URL}} index page
  return c.Render(200, r.Auto(c, {{.ModelName.VarCaseSingular}}))
}