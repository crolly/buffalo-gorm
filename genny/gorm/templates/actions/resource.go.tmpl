package actions

import (

  "github.com/pkg/errors"
  "github.com/gobuffalo/buffalo"
  "github.com/gobuffalo/pop"
  "{{.App.ModelsPkg}}"
)

// This file is generated by Buffalo. It offers a basic structure for
// adding, editing and deleting a page. If your model is more
// complex or you need more than the basic implementation you need to
// edit this file.

// Following naming logic is implemented in Buffalo:
// Model: Singular ({{.Model.Model}})
// DB Table: Plural ({{.Model.Table}})
// Resource: Plural ({{.Name.Resource}})
// Path: Plural (/{{.Name.URL}})
// View Template Folder: Plural (/templates/{{.FilesPath}}/)

// {{.Name.Resource}}Resource is the resource for the {{.Model.Model}} model
type {{.Name.Resource}}Resource struct{
  buffalo.Resource
}

// List gets all {{.Model.ModelPlural}}. This function is mapped to the path
// GET /{{.Name.URL}}
func (v {{.Name.Resource}}Resource) List(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  {{.Model.VarCasePlural}} := &models.{{.Model.ModelPlural}}{}

  // Paginate results. Params "page" and "per_page" control pagination.
  // Default values are "page=1" and "per_page=20".
  q := tx.PaginateFromParams(c.Params())

  // Retrieve all {{.Model.ModelPlural}} from the DB
  if err := q.All({{.Model.VarCasePlural}}); err != nil {
    return errors.WithStack(err)
  }

  // Add the paginator to the context so it can be used in the template.
  c.Set("pagination", q.Paginator)

  return c.Render(200, r.Auto(c, {{.Model.VarCasePlural}}))
}

// Show gets the data for one {{.Model.Model}}. This function is mapped to
// the path GET /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Show(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.Model.Model}}
  {{.Model.VarCaseSingular}} := &models.{{.Model.Model}}{}

  // To find the {{.Model.Model}} the parameter {{.Name.ParamID}} is used.
  if err := tx.Find({{.Model.VarCaseSingular}}, c.Param("{{.Name.ParamID}}")); err != nil {
    return c.Error(404, err)
  }

  return c.Render(200, r.Auto(c, {{.Model.VarCaseSingular}}))
}

// New renders the form for creating a new {{.Model.Model}}.
// This function is mapped to the path GET /{{.Name.URL}}/new
func (v {{.Name.Resource}}Resource) New(c buffalo.Context) error {
  return c.Render(200, r.Auto(c, &models.{{.Model.Model}}{}))
}

// Create adds a {{.Model.Model}} to the DB. This function is mapped to the
// path POST /{{.Name.URL}}
func (v {{.Name.Resource}}Resource) Create(c buffalo.Context) error {
  // Allocate an empty {{.Model.Model}}
  {{.Model.VarCaseSingular}} := &models.{{.Model.Model}}{}

  // Bind {{.Model.VarCaseSingular}} to the html form elements
  if err := c.Bind({{.Model.VarCaseSingular}}); err != nil {
    return errors.WithStack(err)
  }

  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Validate the data from the html form
  verrs, err := tx.ValidateAndCreate({{.Model.VarCaseSingular}})
  if err != nil {
    return errors.WithStack(err)
  }

  if verrs.HasAny() {
    // Make the errors available inside the html template
    c.Set("errors", verrs)

    // Render again the new.html template that the user can
    // correct the input.
    return c.Render(422, r.Auto(c, {{.Model.VarCaseSingular}}))
  }

  // If there are no errors set a success message
  c.Flash().Add("success", "{{.Model.Model}} was created successfully")

  // and redirect to the {{.Name.URL}} index page
  return c.Render(201, r.Auto(c, {{.Model.VarCaseSingular}}))
}

// Edit renders a edit form for a {{.Model.Model}}. This function is
// mapped to the path GET /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}/edit
func (v {{.Name.Resource}}Resource) Edit(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.Model.Model}}
  {{.Model.VarCaseSingular}} := &models.{{.Model.Model}}{}

  if err := tx.Find({{.Model.VarCaseSingular}}, c.Param("{{.Name.ParamID}}")); err != nil {
    return c.Error(404, err)
  }

  return c.Render(200, r.Auto(c, {{.Model.VarCaseSingular}}))
}

// Update changes a {{.Model.Model}} in the DB. This function is mapped to
// the path PUT /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Update(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.Model.Model}}
  {{.Model.VarCaseSingular}} := &models.{{.Model.Model}}{}

  if err := tx.Find({{.Model.VarCaseSingular}}, c.Param("{{.Name.ParamID}}")); err != nil {
    return c.Error(404, err)
  }

  // Bind {{.Model.Model}} to the html form elements
  if err := c.Bind({{.Model.VarCaseSingular}}); err != nil {
    return errors.WithStack(err)
  }

  verrs, err := tx.ValidateAndUpdate({{.Model.VarCaseSingular}})
  if err != nil {
    return errors.WithStack(err)
  }

  if verrs.HasAny() {
    // Make the errors available inside the html template
    c.Set("errors", verrs)

    // Render again the edit.html template that the user can
    // correct the input.
    return c.Render(422, r.Auto(c, {{.Model.VarCaseSingular}}))
  }

  // If there are no errors set a success message
  c.Flash().Add("success", "{{.Model.Model}} was updated successfully")

  // and redirect to the {{.Name.URL}} index page
  return c.Render(200, r.Auto(c, {{.Model.VarCaseSingular}}))
}

// Destroy deletes a {{.Model.Model}} from the DB. This function is mapped
// to the path DELETE /{{.Name.URL}}/{{"{"}}{{.Name.ParamID}}}
func (v {{.Name.Resource}}Resource) Destroy(c buffalo.Context) error {
  // Get the DB connection from the context
  tx, ok := c.Value("tx").(*pop.Connection)
  if !ok {
    return errors.WithStack(errors.New("no transaction found"))
  }

  // Allocate an empty {{.Model.Model}}
  {{.Model.VarCaseSingular}} := &models.{{.Model.Model}}{}

  // To find the {{.Model.Model}} the parameter {{.Name.ParamID}} is used.
  if err := tx.Find({{.Model.VarCaseSingular}}, c.Param("{{.Name.ParamID}}")); err != nil {
    return c.Error(404, err)
  }

  if err := tx.Destroy({{.Model.VarCaseSingular}}); err != nil {
    return errors.WithStack(err)
  }

  // If there are no errors set a flash message
  c.Flash().Add("success", "{{.Model.Model}} was destroyed successfully")

  // Redirect to the {{.Name.URL}} index page
  return c.Render(200, r.Auto(c, {{.Model.VarCaseSingular}}))
}
